overwrite: true
schema: "http://localhost:40501/graphql"
documents: "src/**/*.graphql"
generates:
  src/services/graphql/schema/schema.graphql:
    plugins:
      - schema-ast
    config:
      commentDescriptions: true